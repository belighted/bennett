%tr.build-status-line
  %td= build_information build
  %td= status_image(build.status)
  = build_results_in_tds build
  %td= link_to "Delete", [@project, build], :confirm => "Are you sure you want to delete this build?", :method => :delete, :class => "btn small danger"